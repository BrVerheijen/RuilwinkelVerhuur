@page

@namespace RuilwinkelVerhuur.Models.Classes

@{



    var catlist = new List<string> { "Elektronica", "Meubilair", "Kleren", "Curiosa" };
    var productlisttemp = ProductComm.retrieveList();
    var productlist = new List<Product> { };
    foreach (var i in ViewBag.cart)
    {
        foreach (var j in productlisttemp)
        {
            if (i == j.ID)
            {
                productlist.Add(j);
            }
        }
    }

    int totaalkost = 0;
}

<div class="text-center">
<div class="row justify-content-start ">
                            <div class="col"> <img class="irc_mi img-fluid cursor-pointer " src="https://www.ruilwinkelvaals.nl/ruilwinkelvaals.nl/images/ruilwinkel/logo_ruilwinkel_vaalsWEB.jpg" width="70" height="70"> </div>
                        </div>
    <h1 class="display-4">Mijn Winkelmandje</h1>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Naam
                    </th>
                    <th>
                        Afbeelding
                    </th>
                    <th>
                        Prijs
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>

                @foreach (var product in productlist)
                {
                <tr>
                    
                    @{totaalkost = totaalkost + product.Cost;}
                    <td>
                        @Html.DisplayFor(modelItem => product.Name)
                    </td>
                    <td>
                        <img src="@product.Picture" height="150" />
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => product.Cost)                                   
                    </td>
                    <td>
                        <button onclick="VerwijderProduct()">Verwijder</button>
                                <script>
                                      function VerwijderProduct() {
                                            var txt;
                                            var r = confirm("Weet u zeker dat u dit product wilt verwijderen?");
                                            if (r == true) {
                                               txt = "Product is  verwijderd";
                                                    } 
                                                    

                                           }
                                 </script>
                    </td>
                </tr>
                }
            </tbody>
        </table>

            <p class="mb-1"><b>Totaalprijs: @totaalkost</b></p>


        <div class="row mb-5 mt-4 ">
             <div class="col-md-7 col-lg-6 mx-auto"><button type="button" onclick="CheckOut()" class="btn btn-block btn-outline-primary btn-lg">CHECK OUT</button>
              <script>
                    function CheckOut() {
                                var txt;
                                var r = confirm("Weet u zeker dat u de bestelling wilt afronden?");
                                 if (r == true) {
                                         txt = "Check out is afgerond";
                                               } 
                                                    

                                                }
             </script>
         </div>
        </div>
</div>
